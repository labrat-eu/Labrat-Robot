syntax = "proto3";

package mavlink.msg.common;

message DataStream {
  uint32 message_rate = 1; /*< [Hz] The message rate*/
  uint32 stream_id = 2; /*<  The ID of the requested data stream*/
  uint32 on_off = 3; /*<  1 stream is enabled, 0 stream is stopped*/
}

