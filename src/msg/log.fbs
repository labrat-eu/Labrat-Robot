namespace labrat.robot.msg;

enum Verbosity : byte {
  critical = 0,
  warning = 1,
  info = 2,
  debug = 3,
}

table Log {
  verbosity:Verbosity;
  timestamp:ulong;
  logger_name:string;
  message:string;
}

root_type Log;
